trigger:
  branches:
    include:
      - master  # branch que irei configurar como trigger na pipeline

jobs:
  - job: build
    displayName: 'Build Docker Image on WSL'
    pool:
      name: 'Ryzen-Neto-Local'  # VM local que vai rodar o pipeline
    steps:
      - script: |
          # Certificar-se de que estamos no diretório correto
          cd $(Build.SourcesDirectory)

          # Executar comandos Docker no WSL
          wsl -- docker login -u $DOCKER_USERNAME --password-stdin
          wsl -- docker build -f $(Build.SourcesDirectory)/Dockerfile -t jpneto90/unyleya_projeto_devops:latest .
          wsl -- docker push jpneto90/unyleya_projeto_devops:latest
        displayName: 'Build and Push Docker Image'
        env:
          DOCKER_USERNAME: $(DOCKER_USERNAME)
          DOCKER_PASSWORD: $(DOCKER_PASSWORD)
