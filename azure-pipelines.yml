trigger:
  branches:
    include:
      - master  # Branch que irei configurar como trigger na pipeline, sempre que ela for alterada a pipeline ira rodar

jobs:
  - job: build
    displayName: 'Build and Push Docker Image to Docker Hub'
    pool:
      name: 'Ryzen-Neto-Local'  # Agente local que vai rodar o pipeline
    steps:
      - script: |
          # Certificar-se de que estamos no diretório correto
          cd $(Build.SourcesDirectory)

          # Login no Docker Hub
          echo %DOCKER_PASSWORD% | docker login -u %DOCKER_USERNAME% --password-stdin

          # Build e Push da imagem usando buildx
          docker buildx build --platform linux/amd64 -f Dockerfile -t jpneto90/unyleya_projeto_devops:latest --push .
        displayName: 'Build and Push Docker Image'
        env:
          DOCKER_USERNAME: $(DOCKER_USERNAME)  # Variável de ambiente com o usuário do Docker Hub
          DOCKER_PASSWORD: $(DOCKER_PASSWORD)  # Variável de ambiente com a senha do Docker Hub
